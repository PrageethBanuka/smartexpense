# SmartExpense Production Environment Variables
# Copy this file as .env and fill in the values before running docker-compose

# Database
DB_NAME=smartexpense
DB_USER=smartexpense
DB_PASSWORD=C+G/nNtoeaeU3uyzlHpMZYGAorzVOP5q+v5O5ULHBms=          # REQUIRED — generate with: openssl rand -base64 32

# Authentication
JWT_SECRET=AXmyqs5lwImoggWYoVHafab8Id0q+26tGSgBCxwUOeHpeMaIhZQt2rxlzCqZiCG8
8XtRyjOdGugLze5afzVSkA==           # REQUIRED — generate with: openssl rand -base64 64

# Application
CORS_ORIGIN=http://localhost:8080   # Change to your server IP or domain

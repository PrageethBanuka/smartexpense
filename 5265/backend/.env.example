# Server
PORT=4000
NODE_ENV=development

# Database (use either DATABASE_URL or the individual settings below)
# DATABASE_URL=postgres://user:password@host:5432/smartexpense
DB_HOST=postgres
DB_PORT=5432
DB_NAME=smartexpense
DB_USER=smartexpense
DB_PASSWORD=smartexpense

# Auth
JWT_SECRET=supersecret-change-me

# CORS
CORS_ORIGIN=http://localhost:3000
